import { KeysPipe } from './objectToKeys.directive';

describe('KeysPipe', () => {
  it('should create an instance', () => {
    const directive = new KeysPipe();
    expect(directive).toBeTruthy();
  });
});
